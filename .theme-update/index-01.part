--- a/index.html
+++ b/index.html
@@ -20,6 +20,10 @@
       --text: #f5f7ff;
       --muted: #a8afc3;
       --shadow: 0 18px 60px rgba(0, 0, 0, .34);
+      --accent-contrast: #061018;
+      --overlay-bg: rgba(2, 4, 12, .66);
+      --option-bg: #11152a;
+      --option-text: #ffffff;
       --header-h: 70px;
       --deck-h: 140px;
       color-scheme: dark;
@@ -43,34 +47,6 @@
       transition: background 1.1s ease, color .4s ease;
     }
 
-    body[data-theme="synthwave"] {
-      --accent: #ff4fa3;
-      --accent-soft: rgba(255, 79, 163, .20);
-      --accent-glow: rgba(255, 79, 163, .48);
-      background: linear-gradient(155deg, #0b021a 0%, #2a0b45 48%, #ff2e88 135%);
-    }
-
-    body[data-theme="daylight"] {
-      --accent: #2456e6;
-      --accent-soft: rgba(36, 86, 230, .14);
-      --accent-glow: rgba(36, 86, 230, .26);
-      --glass: rgba(255, 255, 255, .58);
-      --glass-strong: rgba(255, 255, 255, .88);
-      --glass-faint: rgba(19, 32, 64, .055);
-      --border: rgba(25, 43, 80, .16);
-      --text: #10172a;
-      --muted: #566179;
-      --shadow: 0 18px 60px rgba(44, 59, 92, .18);
-      color-scheme: light;
-      background: radial-gradient(circle at 32% 20%, #f4f6fa 0%, #dde3ee 55%, #c3ccdd 100%);
-    }
-
-    body[data-theme="ember"] {
-      --accent: #ffb54d;
-      --accent-soft: rgba(255, 181, 77, .18);
-      --accent-glow: rgba(255, 181, 77, .42);
-      background: radial-gradient(circle at 32% 20%, #3b1408 0%, #1c0805 54%, #0b0302 100%);
-    }
 
     button, select, input { font: inherit; }
 
@@ -135,7 +111,7 @@
       pointer-events: none;
     }
 
-    .brand-wrap, .themes { pointer-events: auto; }
+    .brand-wrap, .theme-picker { pointer-events: auto; }
 
     .brand-line {
       display: flex;
@@ -164,33 +140,47 @@
       text-transform: uppercase;
     }
 
-    .themes {
-      display: flex;
-      gap: 6px;
-      padding: 5px;
-      border-radius: 999px;
-      background: color-mix(in srgb, var(--glass) 70%, transparent);
+    .theme-picker {
+      display: grid;
+      grid-template-columns: auto minmax(170px, 260px);
+      align-items: center;
+      gap: 9px;
+      padding: 5px 7px 5px 12px;
+      border-radius: 14px;
+      background: color-mix(in srgb, var(--glass) 76%, transparent);
       border: 1px solid var(--border);
       backdrop-filter: blur(12px);
       -webkit-backdrop-filter: blur(12px);
-    }
-
-    .theme-btn {
-      border: 0;
-      border-radius: 999px;
-      padding: 8px 12px;
+      box-shadow: 0 8px 28px rgba(0,0,0,.14);
+    }
+
+    .theme-picker-label {
       color: var(--muted);
-      background: transparent;
       font-family: "Chakra Petch", Arial, sans-serif;
-      font-weight: 600;
-      font-size: 11px;
-      letter-spacing: .04em;
-    }
-
-    .theme-btn.active {
-      color: var(--accent);
-      background: var(--accent-soft);
-      box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--accent) 35%, transparent), 0 0 20px color-mix(in srgb, var(--accent-glow) 50%, transparent);
+      font-size: 10px;
+      font-weight: 700;
+      letter-spacing: .12em;
+      text-transform: uppercase;
+      white-space: nowrap;
+    }
+
+    .theme-select {
+      min-width: 0;
+      width: 100%;
+      min-height: 35px;
+      padding: 0 34px 0 11px;
+      color: var(--text);
+      background: var(--glass-faint);
+      font-family: "Chakra Petch", Arial, sans-serif;
+      font-size: 12px;
+      font-weight: 700;
+      letter-spacing: .02em;
+    }
+
+    .theme-select option,
+    .deck-select option {
+      background: var(--option-bg);
+      color: var(--option-text);
     }
 
     #rail {
@@ -397,12 +387,11 @@
     }
 
     .panel-foot .primary {
-      color: #061018;
+      color: var(--accent-contrast);
       background: var(--accent);
       border-color: var(--accent);
     }
 
-    body[data-theme="daylight"] .panel-foot .primary { color: #fff; }
 
     #deck {
       position: fixed;
@@ -461,13 +450,12 @@
     }
 
     .action-btn.solve {
-      color: #061018;
+      color: var(--accent-contrast);
       background: var(--accent);
       border-color: var(--accent);
       box-shadow: 0 0 20px color-mix(in srgb, var(--accent-glow) 42%, transparent);
     }
 
-    body[data-theme="daylight"] .action-btn.solve { color: #fff; }
     .action-btn.solve:disabled { cursor: progress; opacity: .78; }
 
     .deck-selects {
@@ -487,7 +475,6 @@
       text-overflow: ellipsis;
     }
 
-    body:not([data-theme="daylight"]) .deck-select option { background: #11152a; color: #fff; }
 
     .sliders {
       display: grid;
@@ -575,14 +562,13 @@
 
     .seq-chip.done { color: var(--accent); background: var(--accent-soft); }
     .seq-chip.now {
-      color: #061018;
+      color: var(--accent-contrast);
       background: var(--accent);
       border-color: var(--accent);
       transform: scale(1.08);
       box-shadow: 0 0 14px var(--accent-glow);
     }
 
-    body[data-theme="daylight"] .seq-chip.now { color: #fff; }
 
     #hud {
       position: fixed;
@@ -623,7 +609,7 @@
       display: grid;
       place-items: center;
       padding: 24px;
-      background: rgba(2, 4, 12, .66);
+      background: var(--overlay-bg);
       backdrop-filter: blur(9px);
       -webkit-backdrop-filter: blur(9px);
       opacity: 0;
@@ -631,7 +617,6 @@
       transition: opacity .25s ease, visibility .25s ease;
     }
 
-    body[data-theme="daylight"] .overlay { background: rgba(32, 42, 65, .40); }
 
     .overlay.open { opacity: 1; visibility: visible; }
 
@@ -729,8 +714,9 @@
       .tagline { display: none; }
       .brand { font-size: 23px; }
 
-      .themes { gap: 2px; padding: 3px; max-width: 70vw; }
-      .theme-btn { padding: 6px 7px; font-size: 9px; }
+      .theme-picker { grid-template-columns: 1fr; gap: 2px; width: min(52vw, 230px); padding: 4px 5px; }
+      .theme-picker-label { display: none; }
+      .theme-select { min-height: 30px; font-size: 10px; padding-left: 8px; }
 
